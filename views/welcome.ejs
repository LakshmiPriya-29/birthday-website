<%- include('partials/header') %>

<style>
  .welcome-screen {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 90vh;
    background: linear-gradient(135deg, #ffb6c1, #ffe6f0);
    flex-direction: column;
    animation: fadeIn 2s;
  }

  .welcome-screen h1 {
    font-size: 3rem;
    color: #d63384;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.05); opacity: 0.9; }
    100% { transform: scale(1); opacity: 1; }
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
</style>

<div class="welcome-screen">
  <h1>🎉 Happy Birthday Baby! ❤️</h1>
  <p>Get ready for surprises... ❤️</p>
</div>

<script>
  // After 3 seconds, redirect to /cake
  setTimeout(() => {
    window.location.href = "/cake";
  }, 3000);
</script>

<%- include('partials/footer') %>
